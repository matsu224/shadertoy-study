# 学習計画

ShaderToyを実行環境として使い、標準GLSLの基礎を段階的に学ぶための課題一覧。

各課題では、結果を先に予想してからコードを書き、実行結果を自分の言葉で説明できることを目標とする。

## この学習計画の進め方

課題は`01`から順番に、1つずつ進める。最初からすべてのコードを書く必要はない。

各課題では次の流れを繰り返す。

1. 課題名と「作るもの」を確認する
2. 対応するフォルダを作る
3. READMEの基本テンプレートを`shader.frag`へコピーする
4. `render`関数の中へ、その課題で学ぶGLSLを書く
5. 実行前に表示結果を予想する
6. ShaderToyで実行し、予想と比較する
7. 完成したコードを`shader.frag`、実行結果を`preview.png`として保存する
8. 「完了条件」を満たしたら次の課題へ進む

分からなくなった場合は、完成コードを一度に探すのではなく、直前の手順まで画面に正しく表示できているか確認する。

## 課題一覧

| 課題 | 作るもの | 主に学ぶこと |
| --- | --- | --- |
| `01_uv_gradient` | 位置によって色が変わる画面 | 座標、UV、ベクトルの成分 |
| `02_shape_masks` | 円、ぼけた円、リング | 距離、`step`、`smoothstep` |
| `03_repetition_patterns` | 繰り返す円、チェック模様 | `fract`、`floor`、`mod` |
| `04_sine_patterns` | 波状・縞状の模様 | `sin`、周波数、振幅 |
| `05_coordinate_transform` | 移動・拡大縮小・回転した模様 | 座標変換、`mat2` |
| `06_vector_dot` | 方向を明暗で表す画面 | `normalize`、`dot` |
| `07_color_mix` | グラデーションと色付きの円 | `mix`、マスクによる色の合成 |
| `08_procedural_pattern` | 既習内容を組み合わせた模様 | 複数の処理の組み立て方 |
| `09_time_animation` | 時間とともに動く模様 | `iTime`、速度と周波数 |
| `10_scanline_effect` | 動く走査線エフェクト | 既習内容から処理を逆算する方法 |

まずは`01_uv_gradient`だけに取り組む。以降の課題は、前の課題の完了条件を満たしてから進める。

## 01 `uv_gradient`

### 作るもの

画面の左下を暗く、右へ行くほど赤く、上へ行くほど緑になるUVグラデーションを作る。その後、X座標だけの白黒表示や左右反転を試す。

### 最初にやること

- `01_uv_gradient`フォルダを作る
- READMEの基本テンプレートを`01_uv_gradient/shader.frag`へコピーする
- `render`関数の中に`vec2 uv = fragCoord / resolution;`を書く
- `return vec3(uv, 0.0);`でUVを色として表示する
- 表示できたら「実験」の項目を上から1つずつ試す

最初の表示では、左下が黒、右へ行くほど赤、上へ行くほど緑、右上が黄色になれば成功。

### 目的

最初に、

> 「今処理しているピクセルは画面のどこにいるのか」

を数値として扱えるようにする。

シェーダーでは非常に重要。

---

### 最初にもらえる値

ShaderToyは`mainImage`へ、

```glsl
fragCoord
```

を渡す。READMEのadapterがその値を`render`へ渡すため、学習コードでも同じピクセル座標を使える。

これは**ShaderToyから渡される値**。

例えば画面が800×600なら、おおよそ、

```text
左下             (0, 0)
中央             (400, 300)
右上             (800, 600)
```

のような座標。

このままだと解像度によって数値が変わる。

そこで、

```glsl
vec2 uv = fragCoord / resolution;
```

とする。

ここで使っているものは、

```text
vec2
/
変数
```

すべて普通のGLSL。

`resolution` という名前にも特別な意味はない。ただの自分で付けた変数名。

---

### Step 1: UVを色として表示する

UVを色として表示する。

考え方は、

```text
uv.x → 赤
uv.y → 緑
```

。

つまり、

```text
左 → 右
0 → 1

下 → 上
0 → 1
```

が色として見える。

---

### Step 2: 中央を0とする座標を作る

次に、

```glsl
vec2 p = uv * 2.0 - 1.0;
```

を作る。

すると、

```text
uv

左下    (0,0)
中央    (0.5,0.5)
右上    (1,1)


↓

p

左下    (-1,-1)
中央    (0,0)
右上    (1,1)
```

になる。

ここは紙に書いて計算した方がいい。

---

### Step 3: 座標の表示方法を変えてみる

次を一つずつ試す。

```text
uv.xだけを表示
uv.yだけを表示

1.0 - uv.x

uv.xとuv.yを入れ替える

p.xを0〜1に戻して表示
```

例えば、

```text
1.0 - uv.x
```

なら実行前に、

> 左右が反転するはず

と予想する。

---

### この課題で覚えるGLSL

```glsl
float
vec2
vec3
vec4
```

ベクトルに対する、

```glsl
+
-
*
/
```

。

そして、

```glsl
uv.x
uv.y
```

という成分アクセス。

---

### 完了条件

何も見ず、

```text
pixel座標
↓
0〜1
↓
-1〜1
```

まで書ければ終了。

---

## 02 `shape_masks`

ここはかなり重要。

### 作るもの

画面中央からの距離を白黒で確認した後、白い円、境界のぼけた円、リングを順番に作る。

### 目的

数値から、

```text
円
リング
境界
マスク
```

を作れるようにする。

使うGLSLは、

```glsl
length()
abs()
step()
smoothstep()
```

。

全部**GLSL標準の関数**。

---

### Step 1: 距離を見る

前回作った、

```glsl
vec2 p = uv * 2.0 - 1.0;
```

を使う。

ただし、横長や縦長の画面では、この座標のまま円を描くと楕円に見える。まず楕円になることを確認し、その後は次のように画面の縦幅を基準にして縦横比を補正する。

```glsl
vec2 p = (fragCoord * 2.0 - resolution) / resolution.y;
```

そして、

```glsl
float d = length(p);
```

。

`length()` はGLSL。

`vec2` の長さを計算している。

数学的には、

```text
sqrt(x² + y²)
```

。

最初は円を作らず、`d`を白黒表示する。

すると、

```text
中央
p = (0,0)

length = 0

↓

黒
```

中央から離れるほど値が大きくなる。

---

### Step 2: 円

次に、

> 距離0.5以内だけ白

を作る。

ここで、

```glsl
step()
```

を使う。

ただコードを覚えるのではなく、

```text
d < 0.5

なら？

d > 0.5

なら？
```

と考える。

---

### Step 3: 引数を逆にする

必ず、

```glsl
step(0.5, d)
```

と、

```glsl
step(d, 0.5)
```

を比較する。

結果がなぜ反転するか調べる。

---

### Step 4: smoothstep

今度は境界を滑らかにする。

```glsl
smoothstep()
```

もGLSL。

考え方は、

```text
step

0 --------| 1


smoothstep

0 ------ gradual ------ 1
```

。

境界範囲を、

```text
狭くする
広くする
```

ことで見た目がどう変わるか試す。

---

### Step 5: Ring

次に、

```glsl
abs(d - radius)
```

を考える。

例えば、

```text
d = 0.5
radius = 0.5

差 = 0
```

なので、

> 半径0.5の円周上だけ値が小さい

状態になる。

そこに `smoothstep()` を組み合わせてリングを作る。

---

### 完了条件

コードを見ずに、

```text
円
リング
ぼけた円
```

を作れる。

さらに、

> `length()` がなぜ円になるのか

説明できる。

---

## 03 `repetition_patterns`

### 作るもの

同じ円が格子状に並ぶ模様と、白黒のチェック模様を作る。

### 目的

座標を繰り返す考え方を身につける。

使うのは、

```glsl
fract()
floor()
mod()
```

全部GLSL。

---

### Step 1: `fract`

```glsl
fract(2.75)
```

なら、

```text
0.75
```

になる。

整数部分を捨てて小数部分だけ返す。

そこで、

```glsl
fract(uv.x * 5.0)
```

を表示する。

すると、

```text
0 → 1
0 → 1
0 → 1
...
```

が横方向に繰り返される。

---

### Step 2: 2次元で繰り返す

```glsl
vec2 cellUV = fract(uv * 5.0);
```

。

すると画面が、

```text
5 × 5
```

の小さいUV空間に分かれる。

各マスの中ではまた、

```text
0〜1
```

になる。

---

### Step 3: 繰り返す円

02で覚えた円をここに組み合わせる。

各セルのUVを、

```text
0〜1
↓
-1〜1
```

に変換して、

```glsl
length()
```

を使う。

すると円を25個個別に書かなくても、同じ円が繰り返される。

ここが非常に重要。

---

### Step 4: checker

今度は、

```glsl
floor(uv * scale)
```

を試す。

`floor()` もGLSL。

これは、

> 今何番目のセルにいるのか

を整数で取得する感覚。

そこから、

```glsl
mod(...)
```

を使って偶数・奇数を分ける。

---

### 完了条件

「模様をコピーしている」のではなく、

> **座標を繰り返している**

と説明できる。

---

## 04 `sine_patterns`

### 作るもの

横方向または縦方向へ明暗が波打つ模様を作り、波の細かさと強さを変更する。

### 目的

`sin()` をシェーダーでどう使うか理解する。

`sin()` はGLSL。

---

### Step 1: `sin`の値を白黒で表示する

まず、

```glsl
float v = sin(p.x);
```

。

`sin()` の結果は、

```text
-1〜1
```

になる。

RGBに表示しやすくするため、

```text
-1〜1
↓
0〜1
```

へ変換する。

これは01の復習。

---

### Step 2: frequency

次に、

```glsl
sin(p.x * 2.0)
```

```glsl
sin(p.x * 10.0)
```

を比較する。

数字を大きくするとどうなるか、実行前に予想する。

---

### Step 3: amplitude

例えば、

```glsl
sin(p.x * frequency) * amplitude
```

。

`frequency` と `amplitude` の役割を区別する。

```text
frequency
→ 横方向にどれくらい細かく変化するか

amplitude
→ 値の大きさ
```

---

### Step 4: X方向とY方向の波を組み合わせる

Xだけでなく、

```glsl
sin(p.x * ...)
sin(p.y * ...)
```

を組み合わせる。

例えば足したり掛けたりする。

ここで「数式を組み合わせると模様になる」感覚を作る。

---

## 05 `coordinate_transform`

ここは少し難しくなる。

### 作るもの

同じ長方形または縞模様を、座標の計算だけで平行移動、拡大縮小、回転させる。

### 目的

> 図形そのものを操作するのではなく、座標を操作する

というシェーダーの重要な考え方を学ぶ。

---

### Step 1: 平行移動

例えば円が、

```glsl
length(p)
```

で中央にあるなら、

```glsl
length(p - vec2(0.3, 0.0))
```

ではどうなるか。

実行前に考える。

ここで、

```glsl
vec2(...)
```

は普通のGLSL。

---

### Step 2: Scale

```glsl
p * 2.0
```

してから円を描く。

図形が大きくなるのか小さくなるのか、先に予想する。

ここは直感と逆に感じるかもしれないので重要。

---

### Step 3: Rotation

ここで初めて行列。

```glsl
mat2
```

はGLSLの型。

2D回転行列、

```text
cosθ  -sinθ
sinθ   cosθ
```

を `mat2` で作る。

それを座標に掛ける。

---

### Step 4: 回転が分かる模様で確認する

円だと回転しても見た目が変わらない。

なので、

```text
長方形
縞模様
```

などを回転させる。

---

### 完了条件

次を説明できる。

> 「オブジェクトを回す」のではなく、「模様を評価する座標を回している」。

---

## 06 `vector_dot`

ここから3Dシェーダーへの橋渡し。

### 作るもの

画面上の方向と右向きベクトルの近さを計算し、右側を明るく、左側を暗く表示する。

### 目的

`normalize()` と `dot()` を画面で理解する。

両方GLSL。

---

### Step 1: Direction

```glsl
vec2 dir = normalize(p);
```

。

`normalize()` は、

> 方向を保ったまま長さを1にする

関数。

---

### Step 2: 2つの方向の内積を表示する

固定方向を作る。

例えば、

```glsl
vec2 right = vec2(1.0, 0.0);
```

。

そして、

```glsl
float d = dot(dir, right);
```

。

---

### 予想

画面右側では、

```text
dir ≈ (1,0)

dot ≈ 1
```

。

上や下では、

```text
約90度

dot ≈ 0
```

。

左なら、

```text
逆方向

dot ≈ -1
```

。

これを画面で確認する。

---

### なぜ重要？

後のUnityで、

```hlsl
dot(N, L)
```

を使う。

これは突然出てくる別の技術ではない。

今やった、

```glsl
dot(directionA, directionB)
```

と数学的には同じ。

この段階で内積を方向の類似度として理解しておく。元の学習方針でも、`dot` を名前だけで覚えず、同方向・直交・逆方向と値の関係まで理解することが重要になっている。

---

## 07 `color_mix`

### 作るもの

青から赤へ変化するグラデーションを作り、円形のマスクを使って背景色と円の色を合成する。

### 目的

色を数式で組み合わせる。

ここで重要なのが、

```glsl
mix()
```

。

GLSLの関数。

HLSLなら `lerp()` に近い。

---

### Step 1: `mix`の動きを確認する

```glsl
mix(a, b, 0.0)
```

なら `a`。

```glsl
mix(a, b, 1.0)
```

なら `b`。

```glsl
mix(a, b, 0.5)
```

なら中間。

---

### Step 2: UVとマスクで色を混ぜる

例えば、

```text
左側 → 青
右側 → 赤
```

をUVを使って補間する。

次に、

02で作った円maskを、

```glsl
mix(backgroundColor, circleColor, mask)
```

に使う。

これで、

> maskは単に白黒画像ではなく、2つの値を切り替える係数として使える

ことが分かる。

---

## 08 `procedural_pattern`

ここは新しい関数を学ぶ課題ではない。

### 作るもの

繰り返す円、回転した縞、リングなど、これまでに学んだ処理を2つ以上組み合わせた模様を1つ作る。

### 目的

今まで学んだものを組み合わせる。

使っていいのは基本的に、

```text
length
smoothstep
fract
sin
mat2
mix
```

など、すでに勉強したものだけ。

---

### 課題

例えば、

> 繰り返す円を作り、セルごとに位置をずらし、色を変える。

あるいは、

> 回転した縞とリングを組み合わせる。

完全に同じものを作る必要はない。

ここから初めて、

```text
何を作るか
↓
何が必要か
```

を少し自分で考える。

---

## 09 `time_animation`

ここで初めてShaderToy固有の時間を使う。

### 作るもの

`04_sine_patterns`で作った模様へ時間を加え、一定方向に動くアニメーションを作る。

### ShaderToy固有

```glsl
iTime
```

これは**GLSLには存在しない**。

ShaderToyが、

> Shader開始からの経過時間

を `float` として渡してくれるもの。

---

### GLSLとして考える

学習コード側では、

```glsl
vec3 render(
    vec2 fragCoord,
    vec2 resolution,
    float time
)
```

のように、ただの `float time` として扱う。

ShaderToy固有部分は最後だけ。

```glsl
render(
    fragCoord,
    iResolution.xy,
    iTime
);
```

こうすれば、

```text
iTime
↓
ShaderToy固有

time
↓
自分の普通のfloat変数
```

と明確に区別できる。

---

### 課題

04の `sin()` に、

```glsl
time
```

を加える。

```text
sin(x)

↓

sin(x + time)
```

。

そこから、

```text
speed
frequency
```

を別々に調整する。

---

## 10 `scanline_effect`

ここが最初のまとめ。

### 作るもの

一定間隔の細い横線が時間とともに移動する、走査線風のエフェクトを作る。

### 目的

これまでのGLSLだけで「それらしいエフェクト」を作る。

新しい技術は極力追加しない。

---

### 考える順番

まず、

```text
Y座標がある
```

。

↓

```text
一定間隔で繰り返したい
```

。

↓

```text
fract / sin
```

が使えそう。

↓

```text
細い線にしたい
```

。

↓

```text
step / smoothstep
```

。

↓

```text
動かしたい
```

。

↓

```text
time
```

。

このように、**欲しい見た目から既知のGLSL機能を逆算する**練習にする。

---

## 最初の10課題で出てくるものを分類すると

ここは手元に置いておくといい。

| 名前                       | 種類                        |
| ------------------------ | ------------------------- |
| `float`                  | GLSL                      |
| `vec2` / `vec3` / `vec4` | GLSL                      |
| `mat2`                   | GLSL                      |
| `length()`               | GLSL                      |
| `normalize()`            | GLSL                      |
| `dot()`                  | GLSL                      |
| `abs()`                  | GLSL                      |
| `floor()`                | GLSL                      |
| `fract()`                | GLSL                      |
| `mod()`                  | GLSL                      |
| `sin()` / `cos()`        | GLSL                      |
| `step()`                 | GLSL                      |
| `smoothstep()`           | GLSL                      |
| `mix()`                  | GLSL                      |
| `clamp()`                | GLSL                      |
| `mainImage()`            | **ShaderToy固有**           |
| `fragCoord`              | **ShaderToyのmainImage引数** |
| `iResolution`            | **ShaderToy固有**           |
| `iTime`                  | **ShaderToy固有**           |

この区別はかなり大事。

---

### 当面は使わないShaderToy機能

最初は意図的に、

```text
iMouse
iChannel0
iChannel1
iChannel2
iChannel3
ShaderToy Buffer
Keyboard input
音声入力
```

などを使わない。

`iResolution` は画面サイズを知るためにほぼ必要なので仕方ない。

`iTime` は09から限定的に使う。

それ以外は標準GLSLの計算だけで進める。

つまり最初の学習は、

```text
ShaderToyを勉強する

ではなく

ShaderToyを実行環境にしてGLSLを勉強する
```

という方針で統一するといい。
